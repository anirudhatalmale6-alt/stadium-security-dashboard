apiVersion: 1

datasources:
  - name: StadiumSecurity
    type: postgres
    url: postgres:5432
    database: stadium_security
    user: grafana
    secureJsonData:
      password: grafana123
    jsonData:
      sslmode: disable
      maxOpenConns: 10
      postgresVersion: 1600
    isDefault: true
    editable: true
